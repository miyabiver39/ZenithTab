# Changelog

このファイルは ZenithTab (ZenthTab) の変更履歴です。
[Keep a Changelog](https://keepachangelog.com/ja/1.0.0/) の形式に準拠し、[Semantic Versioning](https://semver.org/lang/ja/) を採用しています。

Chrome ウェブストアのリリースノートは、公開のたびにこのファイルの該当バージョンの内容をもとに作成します。

## [Unreleased]

### Added

### Fixed

## [1.11.5] - 2026-09-24

### Fixed
- クイック検索のカタログの Microsoft Copilot で、質問が入力されないまま空のチャットが開いていたのを修正。Copilot 側がプロンプトインジェクション対策で URL の `?q=` を受け付けなくなったため、URL から質問を渡せる Bing の Copilot Search(`bing.com/copilotsearch`)に切り替えた。1.11.4 で追加済みの Copilot も読み込み時に自動で置き換える

## [1.11.4] - 2026-09-24

### Added
- クイック検索に「カタログから追加」を追加。ショートカットやドックと同じ画面で検索エンジンを選んで追加できる。Web 検索・AI アシスタント(Claude、Perplexity、Google AI モード、Microsoft Copilot、Grok、Le Chat、Felo)・動画・ショッピング・地図/辞書・開発者向け・SNS のカテゴリと、地域ごとのエンジン(Yahoo! JAPAN、Naver、Baidu、各国の Amazon / Wikipedia など)を収録。Gemini は URL から質問を渡せないため、代わりに Google AI モードを収録
- カタログから追加したエンジンは、検索バーでもサイトのアイコンで表示

### Changed
- クイック検索の設定を 1 つのエンジン一覧に整理。★ で既定のエンジンを選び、ゴミ箱で削除する。削除した標準エンジンはカタログから戻せる。従来の「既定のエンジン」ボタン群・「削除済み」チップ・「よく使われるエンジン」チップはカタログに統合し、URL を指定して追加するフォームは「URL を指定して追加」を押したときだけ表示
- スマート回答の入力例一覧を既定で折りたたみ、「入力例を表示」で開くようにした

### Fixed
- クイック検索の設定で「Default Search Engine」の見出しが翻訳されず英語のまま表示されていたのを解消(見出しごと一覧に統合)

## [1.11.3] - 2026-09-24

### Fixed
- 設定ファイルのインポートでページの `id` / `name` を検証していなかったのを修正。名前が文字列でないページを取り込むと、以後の新しいタブが毎回エラー画面になっていた。重複 ID・予約語の ID も除外し、既に保存済みの不正なページ一覧も読み込み時に正規化する。ごみ箱から復元するページも同様に検証(#79)
- 設定ファイルのインポートに上限を追加(ファイル 10 MB・50 ページ・1 ページあたり 200 ウィジェット)。上限を超えるファイルは読み込む前に理由を表示する(#79)
- 言語ファイルの読み込みに失敗したとき、画面の再描画のたびに再試行してエラーログを出し続けていたのを修正。以後は言語切り替えなど明示的な読み込み時にだけ再試行する(#83)

### Changed
- 外観設定の検証を項目ごとの表に変更し、設定項目を追加したときに検証への追加漏れがあると型チェックで検知できるようにした。壁紙・ドック・ショートカットキーも往復テストで保護(#80)
- テスト: 全体実行時にタイムアウトすることがあった検索エンジン設定のテストを分割・高速化(#81)、`chrome.storage.sync` の失敗時やインポートの例外分岐のテストを追加(#82)

## [1.11.2] - 2026-09-22

### Fixed
- ショートカットの `sanitizeConfig` の条件が反転しており、`null` や URL の無い要素がインポート・共有コード経由で保存され、ダッシュボード全体がクラッシュしうる状態だった。タスク・メモのページ配列にも同様のチェックを追加(#70)
- RSS / Atom フィードの記事リンク・画像・配信元 URL を `isSafeHttpUrl` で検証し、`javascript:` / `data:` などが `<a href>` / `<img src>` に到達しないようにした(#72)
- `chrome.storage.sync` から受信した設定(外観・ドック・ショートカットキー)を検証せずに適用していたのを修正。外観は現在値の上に重ねて補完し、旧バージョンの端末が新フィールドを消さないようにした(#73)
- 壁紙 / 外観のインポートを検証せずに保存していたのを修正。`currentWallpaperUrl` に任意の CSS を注入できる経路を閉じ、背景の URL は `url("...")` としてエスケープして描画するように(#74)
- 1 つのウィジェットの描画例外でダッシュボード全体が「全消去リセット」画面になっていたのを修正。ウィジェット単位でエラーを分離し、代わりに小さな「読み込めませんでした」カードを表示。ルートのエラー画面にも「直近のバックアップから復元」を追加(#71)
- RSS / iCal フィードの取得に応答サイズの上限が無かったのを修正(RSS 2 MB、iCal 5 MB)。新しいタブと 30 分ごとのバックグラウンド更新の両方に適用(#78)

### Changed
- アイコンのみのボタン(ウィジェットの設定・削除、ショートカットの編集・削除、検索エンジン切替 など)に `aria-label` を追加し、`title` の英語ハードコードを 7 言語対応に。プレースホルダー数点、「Configure {widget}」ダイアログのタイトル、言語選択の「自動」表記も同様(#75)
- `AppDrawerModal` を `role="dialog"` にし、Tab トラップ・Escape・フォーカス復帰を追加(`ConfirmDialog` と共通の `useFocusTrap` フック化)(#76)
- 7 言語のロケールのうちアクティブな 1 言語だけを読み込むように変更し、メインの JS チャンクを 492 KB → 326 KB(-34%)に削減。ウィジェットの既定タイトル用に軽量な `defaultTitles.ts` を追加(#77)

## [1.11.1] - 2026-09-22

### Fixed
- 設定の同期: 項目サイズを UTF-8 バイト数(キー込み)で判定。日本語のラベルを多く含むドックが 8 KB 判定をすり抜けて同期全体が失敗する可能性があった(#63)
- 共有コード: 入力 64 KB 超を拒否、解凍は 512 KB を超えた時点で中断(解凍爆弾対策)、取り込むウィジェットは 50 個 / ドックは 30 件まで(#64)
- スマート回答: 「1ヶ月後」「1 year later」などの月・年加算で月末日が翌月に繰り越されていた問題を修正(3/31 の 1 か月後 = 4/30、2/29 の 1 年後 = 2/28)(#65)
- カタログ: アイコンの読み込み失敗が、同じ位置に表示される別サイトのタイルに引き継がれていた問題を修正(#68)

### Changed
- ストア掲載文とプライバシーポリシーに、任意の設定同期(`chrome.storage.sync`)の対象範囲と共有コードの内容を追記(#69)
- テスト: `worldClock` と `calendarService.readCached` のユニットテストを追加(#66)、コンポーネントテストの React `act()` 警告を解消(#67)

## [1.11.0] - 2026-09-22

### Added
- **初回セットアップ(3 ステップ)**: 言語と地域 → 興味 → 用途。回答からページテンプレート・ニュースのトピック・関連するショートカット・地域のドックを組み立てる。各ステップはスキップ可、既存ユーザーには表示しない。設定 › 言語に「もう一度セットアップ」(実行前に自動バックアップ)(#58)
- **サイト / フィードのカタログ**: ショートカット・ドック・ニュースの設定に「カタログから選ぶ」。7 地域 × 30〜55 サイト、フィード 12〜24 件。地域は UI 言語と独立に切替可。既定のドック / ショートカットはこのカタログから導出(内容は従来どおり)(#57)
- ショートカットの取り込み元に **Chrome ブックマーク** と **よく見るサイト**(任意権限)を追加。リンクやタブの URL をショートカットウィジェットに**ドラッグ&ドロップ**で登録(#60)
- **ページテンプレート**: 「ページを追加 › テンプレートから」で 標準 / 仕事 / 学習 / ニュース / ミニマル(#59)
- 初回セットアップ後の最初の 5 タブに、⚙ とレイアウト編集の場所を示す 1 行のヒント(#60)
- 天気: 既定の都市のままなら「現在地の天気を表示しますか?」を 1 度だけ提案(#61)
- 検索バーのスマート回答に **日付加算**(「30日後」「in 2 weeks」)・**曜日**(「2026-12-25 は何曜日」)・**世界時計**(「ロンドンの時間」「time in Tokyo」、主要都市 60 以上)を追加(#61)
- ヘッダーに **今日のまとめ**(未完了タスク · 今日の予定 · 直近のカウントダウン)。全ページを対象、カレンダーはキャッシュのみ参照(#61)
- **共有コード**: 設定 › バックアップから今のページ(と任意でドック)を短い `zt1.` コード / QR に。メモ・タスク・習慣履歴・カレンダー URL・カウントダウン・天気の位置は除外。受け取り側は新しいページとして追加(#62)
- **設定の同期(任意)**: 外観・ドック・ショートカットキーを `chrome.storage.sync` で同期。既定オフ、8 KB 超の項目はスキップして通知(#62)

### Changed
- モーダルを `<body>` にポータル化し、重ねて開いたときは最前面だけが Esc / Tab を受ける(設定内からカタログを開けるように)
- ウィジェットカードに `data-widget-card="<type>"` を付与(E2E 用)

### Fixed
- 1.10.5 の天気ウィジェットのコメントに混入していた cp932 の文字を修正

## [1.10.5] - 2026-09-22

### Fixed
- 天気: にわか雨(WMO 80〜82)が雪の結晶アイコンで表示されていた問題を修正。霧・雷雨にも専用アイコンを割り当て、夜間の晴れは月のアイコンに。着氷性の霧雨・雨(56/57/66/67)の表記も追加(#55)
- 設定: 「バックアップ & 同期」のバックアップ一覧と「ごみ箱」の一覧を、それぞれのリスト内でスクロールするように変更。件数が増えてもエクスポート/インポート/リセットや「ごみ箱を空にする」が常に見える位置に。バックアップの保持ルール(自動 10 件・手動 5 件・各種「〜前」3 件、合計 20 MB)を一覧の下に表示(#56)

## [1.10.4] - 2026-09-21

### Fixed
- 集中タイマーが別タブで作業中に進まない・完了しない問題を修正(#53)。残り時間を終了時刻から算出し、タブに戻った瞬間に実経過時間を反映。停止中に設定の時間を変えると表示に即反映
- カレンダー: 数年前に作成した繰り返し予定(毎日の朝会など)が展開の上限に達して表示されない問題を修正。表示範囲の直前から展開し、スキップ分も COUNT に数える(#48)
- カレンダー: 複数カレンダーを並列取得したとき、後から完了したフィードが先のキャッシュを上書きして消していた競合を解消。RSS のキャッシュも同じ方式に(#49)
- カレンダー: 設定でフィードの色や名前を変えても再取得までイベントに反映されなかった問題を修正(#50)
- ウィジェットが内部設定を保存したとき(天気の現在地検出、QR の入力など)に、開いている設定画面が勝手に閉じてしまう問題を修正(#47)
- 検索: カスタム検索エンジンに `javascript:` などの危険なスキームを登録できてしまう問題を修正。`http(s)` のみ許可し、スキーム省略時は `https://` を補完、保存時にも再検証(#51)
- ドックを 0 件にしたエクスポートをインポートしても以前のドックが残ってしまう問題を修正(#52)
- 保存データの `pageData` に不正なレコードが混入していると起動時にクラッシュしていた問題を修正(該当レコードを読み飛ばす)(#54)

## [1.10.3] - 2026-09-21

### Changed
- パフォーマンス: ストアの購読をフィールド単位のセレクターに統一(#41)。時計の秒更新やメモの入力で、無関係なウィジェットカードや翻訳フックが再描画されなくなった
- パフォーマンス: 本番バンドルをベンダー別チャンク(react / grid / icons / xml / qrcode)に分割し、設定・ウィジェット追加・ウィジェット設定・アプリドロワーの各ダイアログを初回オープン時に遅延ロード(#40)。最大チャンクは 824 kB → 388 kB
- アクセシビリティ: 共通モーダルに `role="dialog"` / `aria-modal` / タイトル参照を付与し、閉じるボタンに名前を追加。Tab / Shift+Tab がモーダル内で循環し、閉じると開いた要素にフォーカスが戻る(#45)。共通 Input のラベルをコントロールに関連付け
- ストア掲載文: 計算機能を検索バーのスマート回答として記述(#46)

### Fixed
- Atom フィードに日付を解釈できないエントリがあると、フィード全体が空になっていた問題を修正(日付だけ省略して他のエントリを表示)(#44)

### Internal
- `SettingsPanel.tsx`(1,355 行)をタブごとのコンポーネント(`layout/settings/`)に分割(#42)
- `useDashboardStore.ts`(1,087 行)を Zustand の Slices パターン(`store/slices/`)に分割。公開インターフェースは不変(#43)
- ウィジェット設定フォームと `rssParser` の分岐テストを追加、React 19 の `act(...)` 警告を解消(#44)

## [1.10.2] - 2026-09-21

### Added
- 検索バーのスマート回答の使い方をアプリ内で確認できるように(#39)。検索欄の ✨ ボタン、または `?` の入力で「検索バーでできること」(種類ごとの入力例)を表示し、例をクリックするとそのまま検索欄に入る。検索ウィジェットの設定にも同じ一覧(クリックでコピー)を掲載。プレースホルダーにも `?` の案内を追加

### Changed
- CI: GitHub Actions を Node 24 対応版(checkout v7 / setup-node v7 / upload-artifact v7 / action-gh-release v3)に更新し、Node 22・`ubuntu-24.04` に固定(#38)

## [1.10.1] - 2026-09-20

### Changed
- ストアの短い説明(`_locales` の `extDescription`)を更新し、カレンダー・習慣トラッカーなど 1.9〜1.10 の機能を反映(#37)。あわせて掲載文(7 言語)と PRIVACY.md をクイックアクセス / カレンダー / スマート回答 / Undo・ごみ箱・バックアップ / 任意権限 `tabs` の内容に更新

### Internal
- README を 1.10 の内容に全面更新し、6 言語の翻訳(`docs/readme/`)とバッジ付きヘッダーを追加(#34, #36)。翻訳の構成ズレを検出するテストを追加
- VS Code 用の `tasks.json` / `launch.json` / `settings.json` / `extensions.json` を追加(#35)

## [1.10.0] - 2026-09-20

### Added
- ウィジェットの最小サイズを見直し、**横 2 列**(検索のみ 3 列)・**縦 1〜2 行**まで縮められるように(#32)。時計・カウントダウン・ショートカットは 1 行まで。最小サイズはレジストリの定義を優先するため、以前のバージョンで追加したウィジェットもそのまま縮められる。1 行の高さではヘッダーと余白を詰め、時計は時刻のみ、カウントダウンは 1 行表示に自動で切り替わる

### Fixed
- カレンダーウィジェットに Google カレンダーの埋め込みページ URL(`…/embed?src=…`)や「URL で追加」のリンク(`…/r?cid=…`)を貼ると取得に失敗していた問題を修正(#33)。保存時に .ics のリンクへ自動変換し、設定画面で変換後の URL を表示、.ics らしくないリンクには注意を出す。取得結果が iCal でない場合はその旨を表示して設定へ誘導する

## [1.9.0] - 2026-09-20

### Added
- **カレンダーウィジェット**(#28): Google カレンダー / Outlook / Apple の iCal(.ics)リンクを貼るだけで、今日〜1・3・7 日先の予定を日付ごとに一覧。複数カレンダー(色分け)、終日・繰り返し(毎日 / 毎週 BYDAY / 毎月 / 毎年、INTERVAL・COUNT・UNTIL)・例外日(EXDATE / RECURRENCE-ID)に対応。`webcal://` は自動で `https://` に変換し、取得元のホストは保存時に 1 回の許可ダイアログで求める(RSS と同じ仕組み、15 分キャッシュ)
- **習慣トラッカーウィジェット**(#30): 日課を毎日チェックして連続日数(🔥)を表示。直近 7 日のドット、今日の達成率バー。今日未達成でも昨日まで続いていればストリークは維持
- **カウントダウンウィジェット**(#31): 名前と日付だけで「あと N 日」を表示。近い順に並び、1 件のときは大きく表示。毎年繰り返し(誕生日・記念日、2/29 は平年 2/28 扱い)
- **検索バーのスマート回答**(#29): 検索欄に打つとその場で答えるカードを表示 — 計算(`120*1.1`、`(3+4)^2`、`sqrt(2)`)、パーセント(`20% of 150`)、単位変換(`10 km to mi`、`72 f to c`、`1 GB to MB`)、進数(`0xff`、`255 to hex`)、サイコロ(`2d6`)、コイントス、乱数(`random 1-100`)、選択(`choose A, B, C`)、日数(`2026-12-31 まで`)。クリックでコピー、ランダム系は「もう一度」。Enter は従来どおり検索。検索ウィジェットの設定でオフにできる
- **壁紙に合わせた文字色の自動調整**(#24): 壁紙の明るさを判定し、明るい壁紙ではヘッダー・ページ帯・検索エンジンのピルなど壁紙の上に直接載る文字を暗色に切り替え、ガラス面も少し濃くする。外観タブでオフにできる(既定オン)
- **組み込みショートカットの一覧**(#25): 設定 > ショートカットキー の先頭に `/`・`Ctrl+Alt+←/→`・`Ctrl+Z` / `Ctrl+Shift+Z`・`Esc` などの組み込みショートカットをキーキャップ表示で掲載(macOS では ⌘ / ⌥ 表記)

### Fixed
- QR コードウィジェットで、横長・縦長にリサイズすると QR コードが引き伸ばされて歪む問題を修正。空き領域の短辺に合わせた正方形で表示する(#26)
- クイックアクセスの「最近閉じたタブ」に何も表示されない問題を修正(#27)。Chrome は `tabs` 権限が無いと閉じたタブの URL / タイトルを返さないため、`tabs` を任意権限に追加し、「最近閉じたタブ」を開いたときだけ追加で許可を求める(「よく見るサイト」は従来の権限のまま動作)

## [1.8.1] - 2026-09-17

### Fixed
- 集中タイマー（ポモドーロ）ウィジェットの下部で「完了セッション数」が見切れてしまう問題を修正。標準の高さ（2行）でもカード枠内にすっきり収まるよう余白・文字サイズ・コンポーネント配置を最適化

## [1.8.0] - 2026-09-17

### Added
- 誤操作からの復元(フールプルーフ対策)(#21)
  - **元に戻す**: ウィジェット / ページ / ドック / キーボードショートカットの削除、ドックの並べ替え、タスク・ショートカットタイルの削除、メモページを閉じる操作、ウィジェットのドラッグ / リサイズを、直後のトースト「元に戻す」または `Ctrl+Z`(`Cmd+Z`)で取り消せる(20 件まで、`Ctrl+Shift+Z` / `Ctrl+Y` でやり直し)。複数タブで同時に使っていても、他タブの変更を巻き戻さない差分復元
  - **ごみ箱**: 削除したウィジェットとページは 30 日間(最大 50 件)保持され、設定 > ごみ箱から元のページ・元の位置に復元できる。エクスポートにも含まれ、リセットしても消えない
  - **自動バックアップ**: リセット / インポート / バックアップ復元の直前、および 30 分以上空いたあとの最初の変更の直前に、ダッシュボード全体のスナップショットを保存(自動 10 世代、手動 5 世代、合計 20MB まで)。設定 > バックアップ & 同期から任意の時点に戻せる(復元自体も戻せる)。カスタム壁紙の画像は重複保存せず、復元時は現在の画像を維持。アップデート前の自動バックアップも同じ一覧に表示
- ウィジェットのあるページの削除、リセット、インポート、ごみ箱を空にする / バックアップの削除は、`window.confirm` に代わるアプリ内の確認ダイアログ(既定フォーカスはキャンセル側)で確認する

### Changed
- 空のページの削除は従来どおり確認なし(トーストから元に戻せる)
- インポート後は編集モードを解除する

### Fixed
- ウィジェット追加やページ削除の直後にタブがフリーズし、再インストールするまで復帰できなくなる問題を修正(#20)。新規ウィジェットの配置に `y: Infinity` を使わず各ブレークポイントの最下段を計算し、保存済みレイアウトの `null` / `NaN` / `Infinity` を読み込み時・保存時に有限値へ正規化する(既に壊れているデータも自動修復)

## [1.7.1] - 2026-09-16

### Changed
- 依存関係の更新: `fast-xml-parser` 5.11 / `vitest` 5(`npm audit` の moderate 4件を解消。実害のあるものはなし)(#19)
- 設定画面のチェックボックス・スライダー・ファイル入力、アイコンのみの削除ボタン、検索エンジンのドロップダウン、集中タイマーの進捗バーにアクセシビリティ属性(aria-label / role)を追加(#18)

### Internal
- ESLint(flat config + typescript-eslint + react-hooks)を導入し CI で実行(#15)
- Playwright E2E を `npm run test:e2e` 一発で実行可能にし、9 シナリオを CI の別ジョブで実行(#16)
- コンポーネントテストを `@testing-library/user-event` とロールベースのクエリに移行(#18)
- CHROMEWEBSTORE.md の Version History と CHANGELOG のリンク定義を 1.7.0 まで同期(#17)

## [1.7.0] - 2026-09-16

### Added
- 保存データのスキーマバージョニングとマイグレーション基盤(#14)。`schema_version` を記録し、将来の破壊的変更を「1版ずつ前進・冪等・直前スナップショット付き」で適用できる仕組みを導入(現在はバージョン 1、変換なし)。エクスポート JSON にも `schemaVersion` を記録し、インポート時に同じパイプラインを通す
- 旧バージョン(v1.2.3 / v1.3.3 / v1.5.0)の実データを使ったアップグレード回帰テスト(#13)。自動更新経路とインポート経路の両方で、初期化・全画面描画・全ウィジェットの設定モーダルが例外なく動くことを保証
- `unlimitedStorage` 権限(警告なし)。カスタム壁紙やフィードキャッシュで 10MB 上限に当たり、保存が黙って失敗する可能性を排除

### Fixed
- 旧バージョンで保存されたウィジェット設定・壁紙・外観に、その後追加されたフィールドが欠けたままになる問題を修正(読み込み時に既定値で補完。ユーザーの値や空にした配列は維持)(#13)
- v1.3 以前の RSS ウィジェット(キーワード検索)が、既定モードの補完によってトップニュース表示に置き換わってしまう問題を、レジストリの `migrateConfig` フックで回避
- バックグラウンドの RSS 更新がアクティブページのウィジェットしか対象にしていなかった問題を修正(全ページを走査)

## [1.6.1] - 2026-09-16

### Fixed
- **重要**: v1.6.0 で必須権限に追加した `topSites` / `sessions` を任意権限(`optional_permissions`)に変更。警告付き権限を必須に足すと Chrome は自動更新時に拡張を無効化するため、そのまま公開すると既存ユーザーの新しいタブが Chrome 標準に戻ってしまう問題を未然に修正(v1.6.0 はストア未公開)
- クイックアクセスウィジェットは追加時に権限を要求し、未許可の間は「権限を許可する」ボタンを表示。拡張内で未許可のときにモックデータが出ていた問題も修正

### Changed
- ウィジェットレジストリ化のリファクタリング(`WidgetConfigModal` 893行→70行、各ウィジェットの設定フォームを分離、`switch (widget.type)` を全廃)。振る舞いの変更なし
- `CLAUDE.md` を実体化(AI 向け開発手順、ウィジェット追加の4ステップ、リリース手順)

## [1.6.0] - 2026-09-16

### Added
- クイックアクセスウィジェット(#12)。Chrome が記録している「よく見るサイト」と「最近閉じたタブ」をタブ切替で表示し、ワンクリックで開く / 復元。リスト・グリッド表示、5/8/12 件、新しいタブで開くかを設定可能。Chrome 内部 API のみで外部通信なし
- 権限 `topSites` と `sessions` を追加(ストア掲載文の権限説明も追記)

### Fixed
- CHROMEWEBSTORE.md の権限表で bookmarks / alarms / favicon の行頭が制御文字で欠けていたのを修正

## [1.5.0] - 2026-09-16

### Added
- 時間帯連動ダイナミック壁紙(#10)。朝(6時〜)・昼(11時〜)・夕(17時〜)・夜(20時〜)で背景を自動で切り替え
  - 「いい感じにして！」: 設定不要。時間帯ごとに厳選した写真カテゴリ・明るさ・オーバーレイを自動適用(夜は目に優しい暗めのオーバーレイ)
  - 「こだわって設定する」: 時間帯ごとに開始時刻、写真カテゴリ / グラデーション、ぼかし・明るさ・オーバーレイを個別指定
  - 時刻チェックはタブ表示時のみ(常時タイマーなし)、切り替えはクロスフェード、オフライン時は CSS グラデーションにフォールバック。外部接続の追加なし
  - 「壁紙を変更」ボタンは時間帯モードでは同じ時間帯内の次の写真へ

### Fixed
- ショートカット / ブックマークウィジェットで設定した表示列数(2〜8)が反映されず固定列で描画されていた問題を修正(#11)

## [1.4.1] - 2026-09-16

### Added
- 国・言語別の初期プリセット(#9)。初回起動時と「デフォルトに戻す」実行時に、ブラウザ/設定言語に応じたクイックドック(6件)・ショートカット(12件)・天気の初期都市を生成
  - 日本語: Google, YouTube, X, Yahoo! JAPAN, Amazon.co.jp, ChatGPT / 楽天市場, Instagram, Gmail, Google マップ, Spotify, Netflix など(東京)
  - 韓国語: Naver, YouTube, Coupang, ChatGPT, Instagram, Google / Daum, 배달의민족, Musinsa など(ソウル)
  - 中国語: Bilibili, YouTube, Baidu, Taobao, Xiaohongshu, ChatGPT / 微博, 京东, 知乎 など(上海)
  - 英語・スペイン語・フランス語・ドイツ語: Google, YouTube, 各地域版 Amazon, ChatGPT, Google Maps, Wikipedia + 地域ポータル(NY / マドリード / パリ / ベルリン)
  - 未対応言語はグローバル(英語)プリセットにフォールバック
- カテゴリ名を各言語の日常的な表現に(「SNS・動画」「ショッピング」「AI・ツール」など)

### Changed
- 既存ユーザーの保存済みドック・ショートカットはそのまま維持(プリセットは未保存時とリセット時のみ適用)

### Fixed
- ページ・ウィジェット・ドック項目などの ID がタイムスタンプのみで生成されていたため、同一ミリ秒内の連続作成で衝突し得た問題を修正(衝突しない ID 生成に変更)

## [1.4.0] - 2026-09-16

### Added
- Google News ウィジェットに「主要ヘッドライン / トピック別 / キーワード検索」の3モードを追加(#6)。トピックは国際・国内・ビジネス・テクノロジー・エンタメ・スポーツ・科学・健康の8種。設定画面のモード切替に加え、ウィジェット内の検索欄を空にするか「ヘッドライン」等と入力すると主要ヘッドラインに戻る
- `npm run test:coverage` を追加。テスト基盤を刷新し(Chrome API モックの差し替え・自動クリア、stderr ノイズ除去)、ストア・サービス・全ウィジェット・レイアウトのテストを追加(#3、42→290件、ステートメントカバレッジ 23%→98%)

### Fixed
- 言語を切り替えてもウィジェットのヘッダー(時計・天気情報・タスク管理など)、天気の曜日、天候テキストが追従しなかった問題を修正(#7)。ユーザーがリネームしたタイトルは維持
- Google News で「ヘッドライン」と入力すると単語検索になっていた問題を修正(#6)
- Web埋め込みウィジェットにスキーム無しのURL(例: `example.com`)を設定するとダッシュボード全体がエラー画面に落ちる問題を修正。不正な URL は埋め込まず案内を表示し、設定保存時に `https://` を補完
- 初回起動直後の他タブ同期で不要な再描画が起きていた問題を修正

## [1.3.3] - 2026-09-16

### Added
- 空のページにプレースホルダを表示。「何も消えていない」ことと、ウィジェット追加 / 元のページへ戻る導線を明示(ページ追加で「ウィジェットが全部消えた」と見えていた問題への対応)
- ページ追加時に「空のページを追加」「このページを複製」を選べるメニューを追加(ヘッダーのボタンにもラベルを表示)
- 複数の新規タブを開いている場合、他のタブでの変更(ページ追加・ウィジェット編集・設定変更など)を自動で取り込むように(`chrome.storage.onChanged` 監視)。古いタブの操作が新しいタブの変更を上書きしてしまう問題を防止
- ニュースウィジェットでキーワードを空にすると、言語設定に応じた地域の Google News トップニュースを表示

### Changed
- 初回起動時のデフォルト内容(ウィジェット名、クイックメモのウェルカム文、タスクのサンプル、ニュースのタイトル)をブラウザの言語で生成するように。以降は通常のユーザーデータとして扱うため、言語を切り替えても編集済みの内容は変わらない
- デフォルトのニュースを「technology」キーワード検索から地域のトップニュースに変更
- デフォルトのクイックドック / ショートカットを一般向けに変更(GitHub, Dev Docs, Figma, Notion, Reddit → Google Maps, Wikipedia, Instagram, Netflix)
- ページ名を言語設定に追従する「ページ N」表示に(未リネームのページのみ。名前を空にすると自動命名に戻る)
- Google News のフィードURLを7言語すべてで各地域のエディションに対応(従来は日本語以外が `en-US` 扱い)

## [1.3.2] - 2026-09-12

### Changed
- 検索エンジンの管理を組み込み・カスタムで統一。組み込みエンジン(Google, Bing, DuckDuckGo など)も削除可能に(削除したものは「元に戻す」チップから復元可能、最後の1件は削除不可)
- カスタムエンジン追加時に `{query}` プレースホルダが無い場合、追加ボタンが無効になる理由をインライン警告で表示

### Fixed
- 単一ページの状態からページを追加すると、新規タブが読み込み画面のまま固まる可能性があった問題への対策(ストレージ読み込みに5秒のタイムアウトを設け、失敗時はデフォルトで起動)
- 描画クラッシュ時に「デフォルトに戻す」復旧画面を表示するエラーバウンダリを追加
- ウィンドウ幅の変更でブレークポイントが切り替わる瞬間にウィジェットが重なって見える問題を修正

## [1.3.1] - 2026-09-11

### Added
- 検索エンジンのプリセット(Yahoo! JAPAN, Yahoo!, Baidu, Naver, Ecosia, Brave Search)を追加。検索結果URLを貼り付けると `{query}` テンプレートへ自動変換
- 検索エンジンのドロップダウンから直接「検索エンジンを管理」を開けるように
- クイックドックの項目をドラッグで並べ替え可能に。アイコンを22種から91種に拡充
- ページ切り替えのキーボードショートカット(Ctrl+Alt+←/→)を追加
- 挨拶のスプラッシュテキストを各言語108本に拡充

### Changed
- ページが1つだけの間はページタブ行を非表示にし、ヘッダーの小さなボタンから追加できるように
- ニュースのサムネイルが無い記事では、記事元サイトのファビコンを表示

## [1.3.0] - 2026-09-11

### Added
- ダッシュボードの複数ページ対応。ウィジェットとレイアウトをページ単位で切り替え可能に(タブクリックで切替、ダブルクリックでリネーム、+ で新規ページ追加)
- 検索ウィジェットでカスタム検索エンジンを追加可能に(名前・URLテンプレート・任意の絵文字アイコン)
- クイックドックの項目を自由にカスタマイズ可能に(追加・削除・並べ替え、22種のアイコンまたは任意の絵文字)
- 左上の挨拶メッセージがランダムにユーモラスなメッセージへ変化するように(Minecraft/Discordのスプラッシュテキスト風、全7言語対応)
- 任意のリンクにキーボードショートカットを割り当てられる機能を追加(設定 → ショートカットキー)
- クイックメモにページ(タブ)機能を追加。1ウィジェットに最大8ページまでメモを分けて管理可能
- QRコード生成ウィジェットを追加。URL・電話番号・テキストをQRコード化し、ダウンロード・コピーに対応

### Fixed
- 検索バーのエンジン選択ドロップダウンを `document.body` にポータル表示し、下のウィジェットに隠れて見えなくなる問題を解消
- react-grid-layout の `data-grid` 属性がブレークポイント別レイアウトを常に上書きしていた不具合を修正し、ウィンドウ幅を変えてもウィジェットが重なったり崩れたりしないように改善
- ダッシュボード下部の不要な余白を解消(コンテナの二重ストレッチを解消し、Dock が下部にある時だけ余白を確保するよう変更)

## [1.2.3] - 2026-08-25

### Fixed
- ウィジェットのリサイズハンドルを、レイアウト編集モード以外では非表示にした
- ダッシュボードの言語設定に合わせて、時計の日付・時刻表示のフォーマットを修正

### Docs
- ストア掲載文を5言語分追加し、ホスト権限の正当化理由を修正

## [1.2.2] - 2026-08-25

### Fixed
- 検索バーのレイアウトを改善し、デフォルトのウェルカムノートにユーモアを追加
- QuickNotes のデフォルト内容を簡潔な英語に変更
- ストア掲載文と実装の不一致を解消し、権限まわりを堅牢化

### Chore
- パッケージ化スクリプトが Windows の `Compress-Archive` に対応

## [1.2.1] - 2026-08-24

### Fixed
- ストアのキーワードスパムポリシー違反を解消し、掲載文を更新

## [1.2.0] - 2026-08-23

### Fixed
- `host_permissions` を必要な API ドメインのみに制限し、Chrome ウェブストアの審査を高速化

### Docs
- Chrome ウェブストア用のスクリーンショット・プロモーション画像を追加

## [1.1.0] - 2026-08-23

### Added
- App Drawer(アプリドロワー)とショートカット起動用ウィジェットを追加

## [1.0.0] - 2026-08-23

### Added
- 初回リリース(Manifest V3)
- i18n(多言語)対応、現在地ベースの天気表示、検索・ポモドーロ・Todo ウィジェット

[Unreleased]: https://github.com/miyabiver39/ZenithTab/compare/v1.10.4...HEAD
[1.10.4]: https://github.com/miyabiver39/ZenithTab/compare/v1.10.3...v1.10.4
[1.10.3]: https://github.com/miyabiver39/ZenithTab/compare/v1.10.2...v1.10.3
[1.10.2]: https://github.com/miyabiver39/ZenithTab/compare/v1.10.1...v1.10.2
[1.10.1]: https://github.com/miyabiver39/ZenithTab/compare/v1.10.0...v1.10.1
[1.10.0]: https://github.com/miyabiver39/ZenithTab/compare/v1.9.0...v1.10.0
[1.9.0]: https://github.com/miyabiver39/ZenithTab/compare/v1.8.1...v1.9.0
[1.8.1]: https://github.com/miyabiver39/ZenithTab/compare/v1.8.0...v1.8.1
[1.8.0]: https://github.com/miyabiver39/ZenithTab/compare/v1.7.1...v1.8.0
[1.7.1]: https://github.com/miyabiver39/ZenithTab/compare/v1.7.0...v1.7.1
[1.7.0]: https://github.com/miyabiver39/ZenithTab/compare/v1.6.1...v1.7.0
[1.6.1]: https://github.com/miyabiver39/ZenithTab/compare/v1.6.0...v1.6.1
[1.6.0]: https://github.com/miyabiver39/ZenithTab/compare/v1.5.0...v1.6.0
[1.5.0]: https://github.com/miyabiver39/ZenithTab/compare/v1.4.1...v1.5.0
[1.4.1]: https://github.com/miyabiver39/ZenithTab/compare/v1.4.0...v1.4.1
[1.4.0]: https://github.com/miyabiver39/ZenithTab/compare/v1.3.3...v1.4.0
[1.3.3]: https://github.com/miyabiver39/ZenithTab/compare/v1.3.2...v1.3.3
[1.3.2]: https://github.com/miyabiver39/ZenithTab/compare/v1.3.1...v1.3.2
[1.3.1]: https://github.com/miyabiver39/ZenithTab/compare/v1.3.0...v1.3.1
[1.3.0]: https://github.com/miyabiver39/ZenithTab/compare/v1.2.3...v1.3.0
[1.2.3]: https://github.com/miyabiver39/ZenithTab/compare/v1.2.2...v1.2.3
[1.2.2]: https://github.com/miyabiver39/ZenithTab/compare/v1.2.1...v1.2.2
[1.2.1]: https://github.com/miyabiver39/ZenithTab/compare/v1.2.0...v1.2.1
[1.2.0]: https://github.com/miyabiver39/ZenithTab/compare/v1.1.0...v1.2.0
[1.1.0]: https://github.com/miyabiver39/ZenithTab/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/miyabiver39/ZenithTab/releases/tag/v1.0.0
